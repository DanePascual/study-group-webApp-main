{
  "rules": {
    "rooms": {
      "$roomId": {
        "presence": {
          ".read": "auth != null",
          "$userId": {
            ".write": "auth != null && auth.uid == $userId"
          }
        }
      }
    }
  }
}
