{
  "rules": {
    "rooms": {
      "$roomId": {
        "presence": {
          ".read": true,
          "$userId": {
            ".write": "auth != null && auth.uid == $userId"
          }
        }
      }
    }
  }
}
